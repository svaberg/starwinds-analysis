"""THIS FILE defines the pipelines package boundary.

Pipelines are reserved for long chains of processing steps, including multi-file
processing workflows. Current built-in per-file pipelines are `dummy` and
`quicklook2d` (selected via `sw-pipe --pipeline ...`).
"""
